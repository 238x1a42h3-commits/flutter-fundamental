 You have two buttons A & B . When you click on the A button the image will be shown and you click on the B button then your name, phone number, and email will be shown. How will you achieve this problem statement?
Hint 1:- Add the flag to show the different-2 data on click of buttons.
Hint 2:- Add text in the column for showing the name, phone number, and email.
Hint 3:- Add imageView to show the image.
Hint 4:- Add the image to the assets folder.
source code
import 'package:flutter/material.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      title: 'Show Image or Info',
      home: const InfoSwitcherPage(),
    );
  }
}

class InfoSwitcherPage extends StatefulWidget {
  const InfoSwitcherPage({super.key});

  @override
  State<InfoSwitcherPage> createState() => _InfoSwitcherPageState();
}

class _InfoSwitcherPageState extends State<InfoSwitcherPage> {
  bool showImage = true; // flag to control what to display

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Two Button Demo'),
        backgroundColor: Colors.blue,
      ),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            // Buttons A & B
            Row(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                ElevatedButton(
                  onPressed: () {
                    setState(() {
                      showImage = true; // show image
                    });
                  },
                  child: const Text('A'),
                ),
                const SizedBox(width: 20),
                ElevatedButton(
                  onPressed: () {
                    setState(() {
                      showImage = false; // show user details
                    });
                  },
                  child: const Text('B'),
                ),
              ],
            ),

            const SizedBox(height: 40),

            // Conditional content
            if (showImage)
              // Image section
              Image.asset(
                'assets/profile.png',
                width: 200,
                height: 200,
                fit: BoxFit.cover,
              )
            else
              // Text section
              Column(
                children: const [
                  Text(
                    'Name: Lakshmi Prasanna',
                    style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
                  ),
                  SizedBox(height: 10),
                  Text(
                    'Phone: +91 9876543210',
                    style: TextStyle(fontSize: 18),
                  ),
                  SizedBox(height: 10),
                  Text(
                    'Email: lakshmi@example.com',
                    style: TextStyle(fontSize: 18),
                  ),
                ],
              ),
          ],
        ),
      ),
    );
  }
}
